INET NOPS UAT - IP / Allowlist Details For INET
Prepared for INET support
Date: 2026-06-27
Project: POS Preview / SST iPOS

This file is safe to send to INET support.
No Merchant Key, token, or secret value is included.

============================================================
SHORT ANSWER FOR INET
============================================================

The POS Preview production system is deployed on Vercel.

Production domain:
https://sstipos-ten.vercel.app

Current Vercel project:
sstipos

Current production deployment inspected:
dpl_7t38NTU3zqwuTrW83iKRgN7TaGKM

Current DNS A records resolved for production domain on 2026-06-27:

sstipos-ten.vercel.app
- 216.198.79.3
- 64.29.17.3

Current DNS A records resolved for current deployment domain on 2026-06-27:

sstipos-pknue03f2-sstdevelopaminnos-projects.vercel.app
- 216.198.79.3
- 64.29.17.3

Important:
At this time, we cannot provide a single fixed outbound source IP address
for requests from the POS server to INET, because the project is currently
running on standard Vercel serverless infrastructure.

For IP allowlisting, the correct option is one of these:

1. Enable Vercel Static IPs for this project and then provide INET with
   the assigned static outbound IP pair from Vercel Project Settings.

2. Route INET API calls through a fixed outbound proxy / NAT gateway,
   then provide INET with that proxy/NAT public IP.

Please do not allowlist the DNS/IP of https://sstipos-ten.vercel.app as
the outbound source IP. That domain is for inbound traffic to the POS app
through Vercel's edge/CDN and is not the same as the server-side outbound
egress IP used when POS calls INET.

If INET needs an inbound server/domain reference, use:

Domain:
sstipos-ten.vercel.app

Currently resolved A records:
216.198.79.3
64.29.17.3

If INET needs the outbound source IP for POS-to-INET API calls, the values
above are not sufficient. A fixed outbound IP must be assigned first through
Vercel Static IPs or a fixed outbound proxy/NAT.

============================================================
PUBLIC URLS TO REGISTER / CONFIRM WITH INET
============================================================

Result page / apUrl:
https://sstipos-ten.vercel.app/payment/inet/result

Server-to-server callback URL:
https://sstipos-ten.vercel.app/api/payments/inet/callback

If INET allows by domain instead of IP, please allow:
sstipos-ten.vercel.app

============================================================
OUTBOUND REQUESTS FROM POS TO INET
============================================================

The POS server calls INET UAT using these URLs:

1. OAuth
POST https://new-ops-poc.inet.co.th/uat/oauth/api/v1/oauth-token

2. Access Token
POST https://new-ops-poc.inet.co.th/uat/api/v1/sandbox/payment-transactions/access-token

3. Create Payment
POST <CREATE_PAYMENT_LINK returned by Step 2>

Current failure:

Step 1 OAuth passes.
Step 2 Access Token passes.
Step 3 Create Payment returns:

HTTP 403
Content-Type: text/html

POS error:
inet_nops_create_payment_http_403:html_response

============================================================
CURRENT REQUEST TO INET
============================================================

Please confirm which allowlist method INET requires:

1. Can INET allowlist by domain?
   Domain:
   sstipos-ten.vercel.app

2. If INET requires source IP only, please confirm that a fixed outbound
   IP is mandatory. We will need to enable Vercel Static IPs or add a fixed
   outbound proxy/NAT first, then send the final assigned public IP(s).

3. Please confirm whether the 403 on Step 3 is caused by:
   - missing source IP allowlist,
   - unapproved apUrl domain,
   - UAT Merchant Key not enabled for QR create-payment,
   - or a different required method/header/body for Step 3.

============================================================
VERCEL STATIC IP NOTE
============================================================

Vercel Static IPs are for outbound egress traffic from deployments.
After Static IPs are enabled for the Vercel project, Vercel provides
static outbound IP pairs that can be shared with external services for
allowlisting.

Until Static IPs or another fixed egress route is enabled, the production
POS server does not have a guaranteed single public outbound IP address
that is safe to give to INET for long-term allowlisting.

============================================================
FILES WITH FULL CURL / BODY / PATH DETAILS
============================================================

Full text details:
docs/INET-NOPS-CURL-FOR-INET.txt

PDF version:
docs/INET-NOPS-CURL-FOR-INET.pdf

Handoff note:
docs/INET-NOPS-WAITING-FOR-INET-HANDOFF.md
